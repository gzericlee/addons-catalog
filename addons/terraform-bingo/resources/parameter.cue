parameter: {
	//+usage=SSO地址
	SSO_ENDPOINT: *"" | string
	//+usage=IAM颁发的用户凭证
	ACCESS_TOKEN: *"" | string
	//+usage=CMP地址
	CMP_ENDPOINT: *"" | string
	//+usage=IAM颁发给CMP的客户端凭证
	CMP_CLIENT_SECRET: *"" | string
}
